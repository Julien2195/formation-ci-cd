name: "Mon premier worklow"

on: push

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, world"
      
  job2:
    runs-on: ubuntu-latest
    steps:
      - run: ls
      - uses: actions/checkout@v4
      - run: ls
      

